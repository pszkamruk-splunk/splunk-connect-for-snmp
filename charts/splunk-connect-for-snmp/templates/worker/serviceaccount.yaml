{{- if (include "splunk-connect-for-snmp.worker.serviceAccountCreate" .) -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "splunk-connect-for-snmp.worker.serviceAccountName" . }}
  labels:
    {{- include "splunk-connect-for-snmp.worker.labels" . | nindent 4 }}
  {{- with (include "splunk-connect-for-snmp.worker.serviceAccountAnnotations" .) }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
