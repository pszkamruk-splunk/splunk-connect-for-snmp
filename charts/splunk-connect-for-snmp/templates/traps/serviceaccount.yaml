{{- if (include "splunk-connect-for-snmp.traps.serviceAccountCreate" .) -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "splunk-connect-for-snmp.traps.serviceAccountName" . }}
  labels:
    {{- include "splunk-connect-for-snmp.traps.labels" . | nindent 4 }}
  {{- with (include "splunk-connect-for-snmp.traps.serviceAccountAnnotations" .) }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
